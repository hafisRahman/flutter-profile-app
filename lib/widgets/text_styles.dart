import 'package:flutter/material.dart';

Widget headingText({required text}) {
  return Text(text,
      style: TextStyle(
          fontSize: 20, fontWeight: FontWeight.w600, color: Colors.white));
}
Widget normalText({required text}) {
  return Text(text,
      style: TextStyle(
          fontSize: 20, fontWeight: FontWeight.w600, color: Colors.white));
}
